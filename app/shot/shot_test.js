'use strict';

describe('dribbble.shot module', function() {

  beforeEach(module('dribbble.shot'));

  describe('shot controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var shotCtrl = $controller('shotCtrl');
      expect(shotCtrl).toBeDefined();
    }));

  });
});