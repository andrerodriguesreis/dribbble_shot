'use strict';

describe('dribbble.shotsList module', function() {

  beforeEach(module('dribbble.shotsList'));

  describe('shotsList controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var shotsListCtrl = $controller('shotsListCtrl');
      expect(shotsListCtrl).toBeDefined();
    }));

  });
});